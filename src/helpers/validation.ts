export const treeCharsOrMore = (str: string) => str.trim().length >= 3;
export const oneCharsOrMore = (str: string) => str.trim().length >= 1;
export const isStrEmpty = (str: string) => str.trim().length === 0;
